server.port=${app.config.server.microservice.account-management.port}
spring.application.name=${app.config.server.microservice.account-management.name}
eureka.client.serviceUrl.defaultZone=${app.config.server.discovery.url}
spring.devtools.restart.enabled=${app.config.server.microservice.account-management.devtools.restart.enabled}